#ifndef __RING_HANDLE_H
#define __RING_HANDLE_H
#include <ngx_core.h>

// ngx_int_t ngx_http_websocket_init_master(ngx_log_t *log);
// ngx_int_t ngx_http_websocket_init_process(ngx_cycle_t *cycle);
#endif //__RING_HANDLE_H
